{
  "name": "daily-tech-radar",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "generate": "pnpm --filter @tutti-os/daily-tech-radar-generator generate",
    "lint": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  }
}
